<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Heart Failure Prediction</title>
    <style>
        /* Custom CSS */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7f6;
            color: #333;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }
        .container {
            background-color: #ffffff;
            padding: 30px 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px;
            box-sizing: border-box;
        }
        h1 {
            color: #0056b3;
            text-align: center;
            margin-bottom: 25px;
        }
        form {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }
        .form-group {
            display: flex;
            flex-direction: column;
        }
        label {
            margin-bottom: 5px;
            font-weight: bold;
            color: #555;
        }
        input[type="text"],
        input[type="number"],
        select {
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            width: 100%;
            box-sizing: border-box;
        }
        input[type="submit"] {
            grid-column: 1 / -1; /* Span across both columns */
            background-color: #007bff;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }
        input[type="submit"]:hover {
            background-color: #0056b3;
        }
        .prediction-result {
            text-align: center;
            margin-top: 30px;
            padding: 15px;
            border-radius: 8px;
            background-color: #e9ecef;
            border: 1px solid #ced4da;
            font-size: 1.1em;
            font-weight: bold;
            color: #333;
        }
        .prediction-result.positive {
            background-color: #ffe0e6;
            border-color: #ffc0cb;
            color: #dc3545;
        }
        .prediction-result.negative {
            background-color: #e6ffed;
            border-color: #c3e6cb;
            color: #28a745;
        }
        .probability-text {
            margin-top: 10px;
            font-size: 0.9em;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Heart Failure Prediction</h1>
        <form action="/predict" method="post">
            <div class="form-group">
                <label for="age">Age:</label>
                <input type="number" id="age" name="age" required step="any">
            </div>
            <div class="form-group">
                <label for="anaemia">Anaemia (0=No, 1=Yes):</label>
                <select id="anaemia" name="anaemia" required>
                    <option value="0">No</option>
                    <option value="1">Yes</option>
                </select>
            </div>
            <div class="form-group">
                <label for="creatinine_phosphokinase">CPK (creatinine phosphokinase):</label>
                <input type="number" id="creatinine_phosphokinase" name="creatinine_phosphokinase" required step="any">
            </div>
            <div class="form-group">
                <label for="diabetes">Diabetes (0=No, 1=Yes):</label>
                <select id="diabetes" name="diabetes" required>
                    <option value="0">No</option>
                    <option value="1">Yes</option>
                </select>
            </div>
            <div class="form-group">
                <label for="ejection_fraction">Ejection Fraction:</label>
                <input type="number" id="ejection_fraction" name="ejection_fraction" required step="any">
            </div>
            <div class="form-group">
                <label for="high_blood_pressure">High Blood Pressure (0=No, 1=Yes):</label>
                <select id="high_blood_pressure" name="high_blood_pressure" required>
                    <option value="0">No</option>
                    <option value="1">Yes</option>
                </select>
            </div>
            <div class="form-group">
                <label for="platelets">Platelets:</label>
                <input type="number" id="platelets" name="platelets" required step="any">
            </div>
            <div class="form-group">
                <label for="serum_creatinine">Serum Creatinine:</label>
                <input type="number" id="serum_creatinine" name="serum_creatinine" required step="any">
            </div>
            <div class="form-group">
                <label for="serum_sodium">Serum Sodium:</label>
                <input type="number" id="serum_sodium" name="serum_sodium" required step="any">
            </div>
            <div class="form-group">
                <label for="sex">Sex (0=Female, 1=Male):</label>
                <select id="sex" name="sex" required>
                    <option value="0">Female</option>
                    <option value="1">Male</option>
                </select>
            </div>
            <div class="form-group">
                <label for="smoking">Smoking (0=No, 1=Yes):</label>
                <select id="smoking" name="smoking" required>
                    <option value="0">No</option>
                    <option value="1">Yes</option>
                </select>
            </div>
            <div class="form-group">
                <label for="time">Follow-up Period (days):</label>
                <input type="number" id="time" name="time" required step="any">
            </div>
            <input type="submit" value="Predict">
        </form>

        {% if prediction_text %}
            <div class="prediction-result {% if 'Death Event Likely' in prediction_text %}positive{% else %}negative{% endif %}">
                {{ prediction_text }}
                {% if probability_text %}
                    <div class="probability-text">{{ probability_text }}</div>
                {% endif %}
            </div>
        {% endif %}
    </div>
</body>
</html>
